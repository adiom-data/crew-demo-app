# Prod pins the public host. Base stays host-less so previews bind their own
# host per tenant namespace; only this overlay fixes the release hostname.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: crew-demo
spec:
  hostnames:
    - t-crew-demo.infrapad.ai
